const result = (5 > 3) ? true : false;
console.log(result);
console.log('*********************');
// Or operator first check whether first one is true if not then it choose next one.Since here "" i.e empty string is falsy it will print "Hello"
console.log("" || 'Hello');